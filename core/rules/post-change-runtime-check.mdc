---
description: 代码改动后的通用运行验证闭环
alwaysApply: true
---

# Post Change Runtime Check

- 当改动涉及 `scripts/`、`scenes/`、`resources/`、输入链路或布局参数时，必须触发一次运行验证。
- 运行验证优先顺序：
  1. 启动检查（项目可启动，无崩溃）
  2. smoke 检查（关键路径）
  3. 必要时执行打包检查（平台相关改动）
- 在可行情况下，优先“直接运行验证”而不是导出预览帧。
- 每次验证都应输出可追溯证据（命令、结果、关键日志）。
- 如果验证失败，先修复阻断问题，再继续文档回写或发布动作。
